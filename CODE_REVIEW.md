# 全局代码审查记录

审查日期：2026-07-25

## 本轮约束

- 运行功能、API 契约、Prompt 文本与 UI 表现保持不变。
- 人工维护代码文件控制在 500 行以内。
- `dist/`、运行时数据、备份和演示汇报页面不纳入行数限制。

## 本轮可安全处理

- 拆分服务端路由、配置、会话、模型调用和图片 Provider。
- 建立 Figma 插件源码到 `dist` 的确定性构建，消除手工同步。
- 拆分后台与调试页的状态、API、渲染、表单和事件职责。
- 串行化远端配置写入，避免旧配置晚到覆盖新配置。
- 使用 UUID 生成任务 ID，并拒绝不存在的结果选择。
- 修复后台配置内容进入 `innerHTML` 导致的持久化 XSS。

## 后续需产品或部署决策

以下问题会改变兼容性、安全策略或数据模型，本轮只记录：

1. 会话当前没有成员归属字段，无法按成员隔离读写与删除。
2. JSON、multipart、插件图片下载没有明确大小上限。
3. 限流直接读取 `x-forwarded-for`，需要结合实际代理拓扑配置可信代理。
4. 服务端会访问模型返回的图片 URL，需要定义允许的协议、域名和内网地址策略。
5. 环境变量注入的模型密钥可能随配置持久化，需要确定密钥托管方案。
6. JSON 文件存储缺少跨进程锁，多实例部署需要迁移到数据库或共享存储。
7. 图片部分生成成功但数量不足时当前整体失败；是否返回部分结果需要产品决策。

## 明确保留

- 不启用尚未接入主链路的 `resultScorer.ts`。
- 不删除遗留 `/api/tasks` 或根目录 `server.js`。
- 不调整模型 fallback、重试、超时、批次和失败语义。
- 不修改场景 Skill、默认配置、Prompt 的内容与顺序。
